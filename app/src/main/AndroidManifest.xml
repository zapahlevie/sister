<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brotherdev.sister">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".SplashActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".NilaiActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".TugasActivity" />
        <activity android:name=".MateriActivity" />
        <activity android:name=".AbsensiActivity" />
        <activity android:name=".AgendaActivity" />
        <activity android:name=".JadwalActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".TugasListActivity" />
        <activity android:name=".TugasDetailActivity"></activity>
    </application>

</manifest>